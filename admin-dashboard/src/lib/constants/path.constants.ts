export enum Path {
  LOGIN = "/login",
  DASHBOARD = "/",
  PRICING = "/pricing",
  MEMBERSHIPS = "/memberships",
  REWARDS = "/rewards",
  BOOKINGS = "/bookings",
  CUSTOMERS = "/customers",
  CLEANERS = "/cleaners",
}